# Day-0 Bootstrap 개선 계획

## 목표
- 이 리포의 책임을 Day-0 Bootstrap으로 고정한다.
- 핵심 결과:
  - Tailnet 연결 완료
  - Ansible 실행 준비 완료(계정/SSH/sudo)
  - 최소 유틸 설치 완료

## 범위
### 포함
- `post-init/post-init.sh` 안정화/단순화
- Day-0 기준 문서 정리(`README.md`)
- Day-0 기준 환경변수 템플릿 정리(`post-init/.env-example`)

### 제외
- Cloudflare tunnel 관련 기능
- 앱 배포/하드닝/모니터링(Ansible에서 수행)
- `bak/` 하위 파일 개선

## 작업 순서 (체크리스트)
1. [x] 범위/인터페이스 고정
2. [ ] `post-init.sh` 안정성/보안/멱등성 개선
3. [ ] `README.md` Day-0 기준 재작성
4. [ ] `.env-example` Day-0 기준 정리
5. [ ] 검증(정적 점검 + 시나리오 점검)

## 세부 작업
### 1) 범위/인터페이스 고정
- 대상: `post-init/post-init.sh`
- 결정사항:
  - 필수 인자: `--sshkey`, `--tailscale`
  - 선택 인자: `--user`, `--ts-hostname`
- 완료기준:
  - `usage`와 실제 파서 동작이 일치
  - README 인자 표와 스크립트 인자가 일치

### 2) `post-init.sh` 개선
- 개선 포인트:
  - `set -euo pipefail` 적용
  - 에러 처리 및 로그 일관화
  - 민감정보 로그 출력 금지
  - 재실행 안전성(사용자/sudoers/SSH 키/설치 단계)
  - 성공 시에만 완료 플래그 생성
- 완료기준:
  - 누락 인자 시 즉시 실패
  - 첫 실행 성공 + 재실행 시 안전 종료
  - 실패 시 부분 성공 상태가 로그로 식별 가능

### 3) `README.md` 재작성
- 포함 내용:
  - Day-0 목적/비목적
  - 빠른 시작 절차
  - 인자 설명
  - 실패 복구 및 재실행 동작
  - 보안 주의사항(토큰/권한)
- 완료기준:
  - 처음 보는 사용자가 README만 보고 실행 가능

### 4) `.env-example` 정리
- 포함 내용:
  - Day-0에 실제 필요한 변수만 유지
  - 변수 설명을 현재 동작과 일치시키기
- 완료기준:
  - `.env-example` 기반으로 명령 생성 시 불필요 변수 없음

### 5) 검증
- 정적:
  - 가능하면 `shellcheck post-init/post-init.sh`
- 동작 시나리오:
  - 신규 서버 1회 실행
  - 동일 서버 재실행
  - 필수 인자 누락 실패
- 완료기준:
  - 각 시나리오 예상 결과 문서화

## 운영 원칙
- Day-0는 최소 기능만 유지한다.
- Day-1 이후 운영 구성은 Ansible로 이관한다.
- 새 요구사항은 “Day-0 범위에 필요한가?”를 먼저 판단한다.
